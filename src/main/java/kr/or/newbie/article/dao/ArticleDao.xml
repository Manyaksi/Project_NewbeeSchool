<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.newbie.article.dao.ArticleDao">

	<select id="showarticleList" resultType="java.util.Map" >
		select a.article_no,u.nickname, a.program_name, a.subject, a.content, a.category, TO_CHAR(a.write_date,'YY/MM/DD HH24: MI') WRITE, a.hit_count, a.like_count, a.notice
		from users u, article a
        where a.user_no = u.user_no
        order by article_no asc
	</select>
	


</mapper>