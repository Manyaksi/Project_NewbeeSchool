<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.or.newbie.users.dao.UsersDao">

	<select id="joinNewbie" parameterType="users" >
		insert into users(user_no, program_name, id, nickname, photo, location, password, point, grade)
		values(users_no_seq.NextVal, #{progamName}, #{id}, #{nickname}, #{photo}, #{location}, #{password}, #{point}, #{grade})
	</select>
	
</mapper>